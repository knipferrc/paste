const Mutation = require('./Mutation')
const Query = require('./Query')
const Schema = require('./Schema')
const User = require('./User')

const types = [User, Query, Mutation, Schema]

module.exports = types
