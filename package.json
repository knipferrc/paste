{
  "name": "pastey",
  "version": "0.0.1",
  "description": "Simple and elegant blogging",
  "repository": "https://github.com/knipferrc/pastey.git",
  "author": "knipferrc <knipferrc@gmail.com>",
  "license": "GPL-3.0",
  "scripts": {
    "start": "concurrently \"cd server && yarn dev\" \"cd client && yarn start\"",
    "deploy": "cd client && yarn deploy && cd .. && cd server && yarn deploy",
    "format": "cd client && yarn format && cd .. && cd server && yarn format",
    "install:deps": "cd client && yarn && cd .. && cd server && yarn"
  },
  "dependencies": {
    "babel-eslint": "^8.0.0",
    "concurrently": "^3.5.0",
    "eslint": "^4.6.1",
    "eslint-plugin-react": "^7.3.0"
  }
}
