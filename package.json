{
  "name": "pastey",
  "version": "0.0.1",
  "description": "Simple and elegant blogging",
  "repository": "https://github.com/knipferrc/pastey.git",
  "author": "knipferrc <knipferrc@gmail.com>",
  "license": "GPL-3.0",
  "scripts": {
    "start": "concurrently \"cd backend && yarn dev\" \"cd frontend && yarn start\"",
    "deploy": "cd frontend && yarn deploy && cd .. && cd backend && yarn deploy",
    "format": "cd frontend && yarn format && cd .. && cd backend && yarn format",
    "install:deps": "cd frontend && yarn && cd .. && cd backend && yarn"
  },
  "dependencies": {
    "babel-eslint": "^8.0.0",
    "concurrently": "^3.5.0",
    "eslint": "^4.6.1",
    "eslint-plugin-react": "^7.3.0"
  }
}
