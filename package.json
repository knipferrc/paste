{
  "name": "pastey",
  "version": "0.0.1",
  "description": "Simple and elegant blogging",
  "repository": "https://github.com/knipferrc/pastey.git",
  "author": "knipferrc <knipferrc@gmail.com>",
  "license": "GPL-3.0",
  "scripts": {
    "start": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "deploy": "cd frontend && npm run deploy && cd .. && cd backend && npm run deploy",
    "format": "cd frontend && npm run format && cd .. && cd backend && npm run format",
    "install:deps": "cd frontend && npm install && cd .. && cd backend && npm install"
  },
  "dependencies": {
    "babel-eslint": "^8.0.0",
    "concurrently": "^3.5.0",
    "eslint": "^4.7.2",
    "eslint-plugin-react": "^7.3.0"
  }
}
